import { useState, useEffect } from "react"
import { useEth } from '../contexts/EthContext'
import { Box } from '@mui/material'

function Voter({ proposals, setProposals, currentPhase, setWinner }) {

    return (
        <Box>Voter panel</Box>
    )
}

export default Voter